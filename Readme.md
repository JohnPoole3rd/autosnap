# Autosnap
An automatic snapchat snap reply bot. Based on python with tk and selenium. It will create a folder called "autosnapedgedata" in the root of your machine to store cookies and other data for the selenium browser instance.
